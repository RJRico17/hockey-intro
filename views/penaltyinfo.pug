extends layout.pug 

mixin penalty(penalty) 
    h2= penalty.name
    img(src='../img/penalty-gifs/cross-checking.gif')
    h3= penalty.penalty
    p= penalty.description 

block content
    for singlepenalty in penalty
        +penalty(singlepenalty)